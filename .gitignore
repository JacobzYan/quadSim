# Ignore enviornmental variables
build/
reference/
.bkp
quadSimPythonVenv/QSVenv/
.vscode

# Keep test parameter files that exist in bin - later should update this so config files live in automations or somewhere else
# ignore files with no extension(don't track binaries, track config files). !bin/**/ is needed so that git will actually look inside /bin   
bin/**
!bin/**/*.param
!bin/**/




# Ignore OS dependent autogenerated files
.DS_Store
Thumbs.db


# Ignore godot files
.godot/
.import/
*.translation
export.csg
export_presets.cfg

