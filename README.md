Required Dependencies:
	cmake
	g++
	python
	ffmpeg


Setup:
Install cmake, g++
	Reccomended command: sudo apt update && sudo apt install build-essential cmake
Run setup.sh
	Creates a build folder, builds cmake, initializes a python enviornment
Install ffmpeg and vlc media player